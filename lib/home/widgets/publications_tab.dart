part of ukan_main;

/// Onglet Publications avec stories et grille 3 colonnes style Instagram.
class PublicationsTab extends StatelessWidget {
  const PublicationsTab({super.key});

  @override
  Widget build(BuildContext context) {
    // Utilise FeedPage qui a la grille 3 colonnes
    return const FeedPage();
  }
}
